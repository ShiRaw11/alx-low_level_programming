#include "main.h"

/**
 * _isupper - check the code.
 *@c: number to be checked
 * Return: 1 for uppercase letter and 0 for otherwise.
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
